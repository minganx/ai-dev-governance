# Git 项目模板

- `.gitignore`：基于 Python、Node、Agent 项目的常见本地文件整理。
- `.gitattributes`：统一文本文件 LF 换行并标记常见二进制资源。

复制后必须按项目确认运行目录、交付目录、数据目录和需要提交的 IDE 配置。`config/git/gitignore_global` 是设备级全局忽略，不能替代项目根目录 `.gitignore`；项目可能产生且不应提交的文件必须显式写入项目配置。
